import { SeedPage } from "@/features/admin/seed/components/seed-page"
import "./seed.css"

export const metadata = {
  title: "Seed Firestore — Admin",
}

export default function AdminSeedRoute() {
  return <SeedPage />
}
